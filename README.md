# 🤖 Multi-Model LLM Router (Учебный AI-проект)

## 🔍 Описание проекта
Данный проект демонстрирует разработку **мульти-модельного агента (LLM Router)**, который автоматически определяет сложность пользовательского запроса и маршрутизирует его в наиболее подходящую языковую модель.

Цель проекта — показать, как можно **оптимизировать стоимость и latency** при работе с LLM, сохраняя качество ответов за счёт использования **нескольких моделей разной мощности**.

Проект выполнен в **Google Colab** и оформлен в виде **Jupyter Notebook** для наглядности и воспроизводимости.

---

## 🧠 Ключевые идеи проекта
- Использование LLM не только для генерации текста, но и для **принятия решений**
- Разделение запросов на **простые** и **сложные**
- Маршрутизация запросов между:
  - дешёвой моделью
  - мощной моделью
- Подсчёт **оценочной стоимости** и **времени отклика**

---

## 🧠 Используемые технологии
- Python 3  
- OpenAI API  
- Jupyter Notebook / Google Colab  
- Large Language Models (LLM)  
- Prompt Engineering  
- Multi-model routing  
- Latency & cost estimation  

---

## ⚙️ Архитектура решения

### Используемые модели
- **Intent Classifier**  
  Определяет сложность запроса (`simple` / `hard`)
- **Easy Model**  
  Используется для простых запросов
- **Hard Model**  
  Используется для технически сложных запросов

### Общий pipeline
1. Пользователь отправляет запрос
2. Запрос передаётся в классификатор intent
3. На основе intent выбирается модель
4. Выполняется основной запрос
5. Считаются latency и оценочная стоимость

---

## ⚙️ Алгоритм работы

### 1. Классификация сложности запроса
Для каждого запроса сначала вызывается LLM-классификатор, который возвращает одно из двух значений:
- `simple`
- `hard`

### 2. Маршрутизация
- `simple` → используется лёгкая и дешёвая модель  
- `hard` → используется мощная модель  

### 3. Генерация ответа
Выбранная модель генерирует ответ пользователю.

### 4. Анализ эффективности
Для каждого запроса измеряются:
- latency (время ответа)
- количество токенов
- оценочная стоимость

---

## 🧪 Эксперименты

В проекте сравниваются два подхода:

### 🔴 Baseline
Использование **только мощной модели** для всех запросов.

### 🟢 Multi-model Router
Использование **интеллектуальной маршрутизации**:
- простые запросы → дешёвая модель
- сложные запросы → мощная модель

Результат:
- снижение стоимости
- уменьшение latency
- сохранение качества ответов

---

## 📊 Результаты
В ходе экспериментов показано, что:
- мульти-модельный подход существенно снижает затраты
- latency уменьшается за счёт обработки простых запросов лёгкой моделью
- архитектура легко масштабируется под дополнительные модели

---

## 🚀 How to run

### 1. Клонировать репозиторий
```bash
git clone https://github.com/USERNAME/multi-model-llm-router.git
cd multi-model-llm-router

2. Задать API-ключ
Для Google Colab

Добавьте переменную OPENAI_API_KEY в разделе Secrets.

Для локального запуска
```bash
export OPENAI_API_KEY="your_api_key_here"

